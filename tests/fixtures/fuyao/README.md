# Synthetic Fuyao fixtures

Every JSON file in this directory is hand-authored synthetic contract data. The
files are not captured, sampled, or transformed from a live Fuyao response.
They exercise only the documented response envelope, decimal handling, retry
classification, and fail-closed parsing behavior.
